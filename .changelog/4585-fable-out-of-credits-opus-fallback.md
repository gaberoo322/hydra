- fix: Fable running out of weekly usage credits now redirects the autopilot to Opus instead of blocking it — the reap arms a model-scoped exhaustion flag (advisory reasons.fableExhaustedUntil, never .allow/sessionBlockedUntil), the pace-gate exec branch launches the parent with an explicit --model while it is live, and the playbook pre-resolves fable-routed dispatches to the fallback; returns to Fable when the flag expires (#4585)
