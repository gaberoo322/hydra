- fix: the last three producerless decide.py selector inputs are closed — discover_target is rewired off the never-produced target_idle onto target_backfill_idle (the class can fire again), the legacy target_research_due reader is deleted (target_board_research_due is the produced mirror), collect-state.sh now emits skill_prune_board_open/skill_prune_board_saturated so the skill_prune anti-flood cap engages, and the PRODUCERLESS_SIGNALS exemption list is emptied behind a size-0 regrow pin (#4607)
