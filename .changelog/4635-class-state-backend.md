- feat: class-state backend per ADR-0034 §9.2 — heartbeat.py forwards decide.py's per-turn decisions/usage-gate fields onto the POST /autopilot/turn body (forwarded, never re-derived; omitted on stale/empty plans), recordTurn sanitises + persists them, and a new GET /api/autopilot/class-state composes per-class last-fired (max of the signal-last-fired hash and the latest turn snapshot), server-computed cooldown remaining, verdict + 15-minute freshness, slot occupant, starved (budget/stagger streak ≥ PROMOTION_THRESHOLD with transparent global-block turns) and static dead classification from a new zero-I/O producerless-signals leaf re-exported by signal-parity-check (#4635)
