- fix: branch-prune, the watchdog and the doctor playbook resolve the Target through the target-config seam (`HYDRA_TARGET_REPO` / `print-target-facts.ts`) instead of the mothballed `~/hydra-betting` checkout — the daily prune GCs the live Target's worktrees, the watchdog drops its hourly false Kalshi-credential WARNING, and the doctor's timer check derives from `scripts/systemd/*.timer` (#4608)
