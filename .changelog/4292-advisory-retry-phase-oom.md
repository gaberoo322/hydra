- fix(ci): the advisory suite-count gate no longer spawns isolated retry children after the suite passes (their node --test fan-out OOM-killed the required test job, exit 137 — #4043 recurrence); retries run only under SUITE_COUNT_GATE_BLOCKING=1, and the launcher now logs INFRA-KILL lines with a phase tag plus its own pid/pgid and the test child's pid for kill attribution (#4292)
