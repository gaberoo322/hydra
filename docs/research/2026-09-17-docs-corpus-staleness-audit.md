# Docs corpus staleness audit — README, reference, CONTEXT, CONTEXT-MAP, ADR roster vs master

**Wayfinder ticket:** [#4538](https://github.com/gaberoo322/hydra/issues/4538) ·
**Map:** [/docs — a generated, drift-tested system reference page in the cockpit #4537](https://github.com/gaberoo322/hydra/issues/4537) ·
**Date:** 2026-09-17

Audited against master commit `06d30e1cd` (== `origin/master` at audit time). Every
finding below was traced to a path/line on that commit by the author; no finding is
carried over from another document. Line numbers are of the audited files at `06d30e1cd`
(`grep -n` / `sed -n`).

Facts only. No prose rewrites, no page-UI proposals. Dispositions are one of **fix in
place** / **move to `docs/historical/`** / **delete** / **becomes a generated inventory**
(hand-off to [#4542](https://github.com/gaberoo322/hydra/issues/4542)).

---

## 1. Summary

**Total findings: 80** (README 16 · `docs/reference.md` 26 · `CONTEXT.md` 20 · `CONTEXT-MAP.md` 7 ·
`docs/adr/README.md` 6 · `src/api/ENDPOINT-REGISTRY.md` 5). The §3 tables hold 85 rows; the five
marked "— (verified)" (README-15, REF-20, REF-26, REF-29, CTX-10) are spot-checks recorded for
coverage, not findings. Cross-file contradictions in §4 are pairs of these, not extra findings.

| Disposition | Count |
|---|---|
| fix in place | 51 |
| move to `docs/historical/` | 5 |
| delete | 6 |
| becomes a generated inventory (→ #4542) | 18 |

Highest-impact findings (one line each):

1. **The Candidate Feed is documented as live in four places and has been deleted since 2026-07-18** (#3455): `src/anchor-candidates.ts`, `src/api/anchor.ts`, `src/api/queue.ts`, `src/schemas/queue.ts`, `src/backlog/` are gone, yet README:46/79, `docs/reference.md:113-141`, `CONTEXT.md:98`, `CONTEXT-MAP.md:29` present `GET /api/anchor/candidates` / `POST /api/queue` as the live work surface (REF-09, CTX-07, MAP-04, README-03/09).
2. **`docs/reference.md:353-366` states the pre-ADR-0015/0020 merge-authority model** ("T3 — Operator merges", "T4 — Operator only, `operator-approved` label required; CI blocks otherwise") that `ci.yml:131-134` and `scripts/tier-classify.ts:17-21` explicitly retired; it contradicts README:68-69, `CONTEXT.md:74`, and the ADR roster's own 0015/0020 rows (REF-17).
3. **`.github/workflows/deploy.yml` does not exist and never has** (no commit in GitHub history touches that path), yet it is the second entry of `VERIFIER_CORE_PATHS` (`src/untouchable.ts:4`) and therefore repeated verbatim — and drift-test-pinned — in `CONTEXT.md:58`, `docs/reference.md:358`, README:69 and CLAUDE.md; deploy is the `deploy` *job* inside `ci.yml:381` (CTX-01).
4. **`src/api/ENDPOINT-REGISTRY.md` is stale relative to its own as-of stamp**: it names `src/api/anchor.ts`, `queue.ts` (deleted 2026-07-18) and `tasks.ts` (deleted 2026-07-11) under an "as of 2026-09-04" header; its header says 147/47 while its table holds 134 rows / 43 modules; master has 147 routes / 48 routers, with 10 registry rows absent from master and 23 master routes absent from the registry (REG-01..03).
5. **The Target swap is not reflected**: README:5/8, `docs/reference.md:150-151/160/550`, roster rows 0025/0031 say `hydra-betting`; `src/target-config.ts:28-29` defaults to `claw-street-bets` / `gaberoo322/claw-street-bets`, `config/direction/` was swapped to CSB on 2026-09-16 (`991c21228`), and the roster's own 0013 row says CSB is the crucible (README-01, REF-10, ADR-03).
6. **Three "historical" sections in `docs/reference.md` claim modules are "intentionally retained" that are gone**: `src/codex-otel.ts` (deleted 2026-05-24, #581), `src/cost/reconciliation.ts` + `GET /api/cost/reconciliation` (deleted 2026-05-25, #602). 152 + 59 lines of runbook for deleted code sit beside live sections (REF-15, REF-19).
7. **`CONTEXT.md` documents machinery that does not exist**: **Autopilot Focus** (`hydra:autopilot:focus`, `hydra focus set/clear`, decide.py focus bias — zero references in `src/`, `scripts/`, `bin/hydra`), **Feedback File** (`src/pattern-memory/feedback-file.ts`, deleted 2026-07-07), **Research Floor** (`src/scheduler/research-floor.ts`, deleted 2026-05-29), and the Relationships bullet at :247 re-asserts the OpenViking Knowledge Base as a live semantic-search surface that the same file retires at :121-123 (CTX-08/09/16/19, CTX-11).
8. **The README's dashboard model ("four operator questions: Today, Now, Outcomes, Explore", :83) matches neither ADR-0034 (five pages + two detail views) nor `dashboard/src/App.jsx`** (six live pages incl. `/now`, two detail routes, four redirects — `/outcomes` and `/explore` are redirects now) (README-11, ADR-07).
9. **Only two tests pin any of the five files** (`test/adr-roster.test.mts` on roster/CONTEXT-MAP structure; `test/verifier-core-docs-drift.test.mts` on three Verifier-Core sentences). README.md and ENDPOINT-REGISTRY.md have zero test coverage (§2).

---

## 2. Already guarded

Drift tests on master (`ls test/*drift*`), and what each pins:

| Test | Pins | Touches an audited file? |
|---|---|---|
| `test/adr-roster.test.mts` | (1) every `docs/adr/[0-9]*.md` has a roster row and every row resolves to a file; (2) 4-digit numbers unique; (3) every ADR **declares** a Status (three spellings recognised) — it does **not** compare the file's status to the roster's status column; (4) both `CONTEXT-MAP.md` "Relevant ADRs" tables parse, every cited ADR exists, `test/fixtures/adr-area-baseline.json` covers exactly the map's areas, per-area weight ratchet, every ADR is routed to some area. | **Yes** — `docs/adr/README.md` (row↔file), `CONTEXT-MAP.md` (ADR-number cells only). |
| `test/verifier-core-docs-drift.test.mts` (#3819) | `VERIFIER_CORE_PATHS` ↔ `CONTEXT.md` "Verifier Core" entry (:58); ↔ CLAUDE.md T4 row; ↔ `docs/reference.md` "Verifier Core list" sentence (:358) and the T4 table row's file count (:354); ↔ `hydra-qa.md` T4 checklist count. Explicitly does **not** re-check ADR-0015's historical "exactly 5". | **Yes** — three sentences in `CONTEXT.md` / `docs/reference.md`. **Not** README's T4 row (README-07). |
| `test/deploy-drift.test.mts` | `shortSha`/`shasMatch`/`classifyDrift` for the watchdog's deployed-SHA-vs-remote classifier. | No |
| `test/scout-constant-drift.test.mts` | TS `CLASS_COOLDOWN_DAYS` ↔ Python `SIGNAL_COOLDOWNS['scout_orch']`. | No |
| `test/typecheck-test-drift.test.mts` | `tsc -p tsconfig.test.json` error count ↔ committed baseline (#4046). | No |
| `test/vendor-drift-check.test.mts` | playbook `supersedes` headings ↔ `_vendor/` bases (#3994). | No |
| `test/watchdog-deploy-drift.test.mts` | `scripts/hydra-watchdog.sh` DEPLOY DRIFT block (#734). | No |
| `test/watchdog-skill-mirror-drift.test.mts` | watchdog SKILL MIRROR DRIFT block (#3828). | No |
| `test/wire-or-retire-carveout-drift.test.mts` | `hydra-wire-or-retire` playbook carve-out restatements (#3957/#4411). | No |

Other tests that mention an audited filename do so only as an inert fixture string (e.g. `/api/tier?files=README.md`, `shouldSkipMutation("CONTEXT.md")`) — measured with `grep -nE 'README\.md|reference\.md|CONTEXT\.md|CONTEXT-MAP\.md|adr/README|ENDPOINT-REGISTRY' test/*.test.mts`; 24 files match, 22 are fixture strings.

**Zero test coverage:** `README.md` (all 145 lines), `src/api/ENDPOINT-REGISTRY.md` (0 references anywhere under `test/`), `docs/reference.md` except :354 and :358, `CONTEXT.md` except :58, `CONTEXT-MAP.md` except the ADR-number cells, `docs/adr/README.md` except link↔file existence and numbering (its Status and Decision columns are unguarded).

---

## 3. Findings by file

### 3.1 `README.md`

| # | Line | Claim (quoted, short) | What master says (path) | Disposition |
|---|---|---|---|---|
| README-01 | 5 | "The current target is hydra-betting" | `src/target-config.ts:28-29` `DEFAULT_TARGET_NAME = "claw-street-bets"`, `DEFAULT_TARGET_GITHUB_REPO = "gaberoo322/claw-street-bets"`; `config/direction/` swapped to CSB in `991c21228` (2026-09-16, #4479); ADR-0013 Decision 2 amended 2026-09-02 (`docs/adr/README.md:27`). | fix in place |
| README-02 | 8 | "**App**: hydra.clawstreetbets.xyz — the hydra-betting web app" | Same swap as README-01. URL answers HTTP 302 (content not verified). | fix in place |
| README-03 | 46 | "`decide.py` turns system state (the [Candidate Feed](./docs/reference.md) …)" | Candidate Feed implementation deleted 2026-07-18 (`191955357`, #3455): `src/anchor-candidates.ts`, `src/api/anchor.ts` absent; no `/api/anchor/*` route on master (measured route list). | fix in place |
| README-04 | 66 | T1 scope "`config/agents/`, `config/feedback/`" | `config/agents/` does not exist on master (`test -e` fails; `docs/historical/README.md` says archived under `docs/historical/agent-personalities/`). `src/tier-classifier.ts:55` still carries the dead prefix. `config/feedback/` exists (`to-planner.md`, `to-planner-1125-iotest.md`, `retro-artifacts/`) but has no reader in `src/` (only the classifier prefix at `src/tier-classifier.ts:56`). | fix in place |
| README-05 | 67 | T2 scope "Skills, dashboard, `src/anchor-selection/`" | `src/tier-classifier.ts:67-75`: prefixes `.claude/skills/`, `dashboard/`; file `src/anchor-selection.ts` (a *file*, deleted 2026-06-01 `42c22b1e8` #821 — never a directory). Repo has no `.claude/skills/` (`ls -a .claude/` → `settings.json settings.local.json worktrees`); skills are generated from `docs/operator-playbooks/` (not in any T2 list → classify T3 by default). | fix in place |
| README-06 | 68 | T3 scope "watchdog scripts, deploy script" | Classifier has no T3 list — everything not T1/T2/T4 is T3 (`src/tier-classifier.ts:76-93`). Accurate by default; wording implies an explicit list. | fix in place |
| README-07 | 69 | T4 "`ci.yml`, `deploy.yml`, tier classifier, `src/untouchable.ts`" | `src/untouchable.ts:2-11` lists **six** paths; row omits `.github/workflows/deep-qa-gate.yml` and `scripts/tier-classify.ts`. Not pinned by `test/verifier-core-docs-drift.test.mts` (it pins CLAUDE.md/CONTEXT.md/reference.md/hydra-qa.md only). `deploy.yml` — see CTX-01. | fix in place |
| README-08 | 71 | "A hard `$50/day` cost cap lives inside the Verifier Core" | No cap in code. `src/scheduler/heartbeat.ts:136,645`: `HYDRA_DAILY_COST_CAP_USD` is "legacy … retired"; only comment mentions remain (`src/scout/calendar-walk.ts:110`, `scripts/autopilot/bootstrap.sh:798`, `scripts/autopilot/decide.py:762`). Nothing under `VERIFIER_CORE_PATHS` references a dollar cap. | delete |
| README-09 | 79 | "for 'do this next', `POST /api/queue`" | Route absent from master (measured); `src/api/queue.ts` + `src/schemas/queue.ts` deleted 2026-07-18 (#3455). | fix in place |
| README-10 | 80 | "**Feedback files** — edit `config/feedback/` to correct agent behavior" | No reader in `src/` (`grep -rn 'config/feedback' src/` → only `src/tier-classifier.ts:9,56`). `src/pattern-memory/agent-memory.ts:27-31`: the `config/feedback/to-*.md` lifecycle "was retired (write-only after the Codex consumers were deleted — ADR-0006 / #710)". Contradicts `docs/reference.md:542` (see §4-1). | fix in place |
| README-11 | 83 | "organized around four operator questions: Today, Now, Outcomes, Explore" | `dashboard/src/App.jsx:94-119`: live pages `/` (Today), `/health`, `/now`, `/builder`, `/runs`, `/work`; detail `/runs/:runId`, `/dispatch/:dispatchId/transcript`; redirects `/outcomes→/builder` (:117), `/explore→/runs` (:118), `/explore/:tab` (:119), `/autopilot/:runId` (:111). ADR-0034:27-37 inventories five pages + two detail views. | becomes a generated inventory (pages) |
| README-12 | 89 | "Node.js ≥ 22" | `package.json` `engines.node` `>=22.18.0 <23.0.0`; `.nvmrc` `22.23.1` (pin enforced by `test/toolchain-pin.test.mts` per CLAUDE.md). | fix in place |
| README-13 | 113 | "~3,500 tests" | Not runnable in this audit. Static measures: 8,236 `test(`/`it(` call sites (`grep -rhoE '^\s*(test\|it)\(' test/*.test.mts \| wc -l`); `test/fixtures/suite-count-baseline.json` sums 1,962 top-level entries across 466 files. Neither is ~3,500. | becomes a generated inventory (counts) |
| README-14 | 119 | "Runtime dependencies … `express`, `ioredis`, `ws`, `@sentry/node`, `zod` … ([ADR-0014])" | `package.json.dependencies` = `@sentry/node, express, ioredis, pino, ws, zod` (six; `pino` per ADR-0027). CLAUDE.md cites ADR-0005 for the same allowlist. | fix in place |
| README-15 | 90 | "Docker + Docker Compose (Postgres, Redis)" | `docker-compose.yml` services `postgres` (:2), `redis` (:24). Accurate. | — (verified) |
| README-16 | 137 | "the Verifier Core stays operator-only" | Contradicts README:55 ("tier names required *verification depth*, not merge authority") and README:69 ("Deep-QA pass; operator only via an exhausted remediation loop"); `docs/adr/README.md:29,34` (0015/0020). Self-contradiction. | fix in place |
| README-17 | 132 | `docs/historical/` covers "Codex CLI, in-process control loop, Specs, in-process Gate" | `docs/historical/README.md` table has 7 rows (adds legacy personalities, stuckness detector, dollar/cost machinery); directory holds `agent-personalities/`, `hydra-betting/`, `README.md`. Incomplete but not wrong. | fix in place |

Verified accurate in README (not findings): `npm start` exists (`tsc && node dist/index.js`); `.env.example:4` sets `HYDRA_PROJECT_WORKSPACE`; dashboard is React + Vite (`dashboard/package.json`); `dashboard/dist/` exists; `/hydra-review`, `/hydra-hitl-grill`, `/hydra-digest`, `/hydra-doctor` are deployed skills; `docs/quality-gates.md`, `docs/operator-playbooks/hydra-autopilot.md`, `LICENSE` (AGPL-3.0 header) exist; pace-gate timer exists (`hydra-pace-gate.timer` enabled).

### 3.2 `docs/reference.md`

| # | Line | Claim (quoted, short) | What master says (path) | Disposition |
|---|---|---|---|---|
| REF-01 | 9-52 | Redis Keys table — 42 rows presented as live | 15 rows are key builders in `src/redis/keys.ts` with **zero callers** outside that file (`grep -rE 'redisKeys\.<name>\b'` across `src/`): `hydra:anchors:{work-queue,processing,prior-failures,reframe-queue,abandonment-count}` (:19-23), `hydra:task:{id}` / `:evidence` (:17-18), `hydra:backlog:{items,lane,counter}` (:31-33), `hydra:deps:{completed,index}` (:49-50), `hydra:blocked:last-escalation` (:48), `hydra:regression-hunt:last` (:46), `hydra:reports:summary:*` (:37). Anchors/backlog builders were orphaned by #3455. | becomes a generated inventory (Redis keys) |
| REF-02 | 15-16, 40-41, 44, 47, 52 | `hydra:cycle:{id}:agents`, `:costs`, `hydra:proposals:*`, `hydra:plans:cache`, `hydra:pattern-detector:cooldowns`, `hydra:workspace:lock` | No occurrence anywhere in `src/` (226 distinct `hydra:` literals scanned). Proposals subsystem deleted 2026-05-24 (`ec2f751df`, #572). Code has `hydra:cycle:{id}:tasks` and `hydra:cycle:index` instead (`src/redis/keys.ts:20-21`). | delete |
| REF-03 | 42-43 | `hydra:specs:*` / `hydra:specs:index` as live keys | `src/redis/keys.ts:170-174`: "Specs — RETIRED (issue #513). … key builders were removed." Subsystem deleted 2026-05-19 (`49bc8f36c`, #519). | delete |
| REF-04 | 30 vs 509 | :30 lists `hydra:scheduler:daily-spend` as a live key "populated only when the Claude Code harness exposes per-call token usage"; :509 says the same key "was retired (see `docs/historical/`)" | `src/index.ts:39`: "`hydra:scheduler:daily-spend` key (no live writer)"; `src/api/metrics-cost.ts:92` reads it as legacy. Self-contradiction inside the file. | fix in place |
| REF-05 | 54-65 | Event Bus streams `hydra:cycle`, `hydra:proposals`, `hydra:notifications`, `hydra:dlq`; consumer groups "meta, orchestrator, telegram, dlq-processor" | `src/event-bus-stream-keys.ts:45-48` `STREAMS = {NOTIFICATIONS, DLQ}`; :64-66 `RETAINED_STREAMS = {CYCLE}` (no writer — the literal `"hydra:cycle"` appears only there); no `hydra:proposals`; :85-88 `CONSUMER_GROUPS`: notifications→`["telegram"]`, dlq→`["dlq-processor"]` only. Also live but unlisted: `hydra:agent-stream` (`src/redis/keys.ts:391`). WebSocket broadcast verified (`src/index.ts:176`). | becomes a generated inventory (streams) |
| REF-06 | 67-84 | API Endpoints section lists 13 routers | Master mounts **48** routers (`src/api.ts:106-222`, 48 `api.use(create…Router())`), 147 routes. Named-but-deleted: `api/tasks.ts` (2026-07-11 `b1456c44b`), `api/queue.ts` (2026-07-18 #3455), `api/specs.ts` (2026-05-19 #519), `api/proposals.ts` (2026-05-24 #572). Routes listed but absent on master: `GET /cycle/report`, all `/tasks*`, all `/queue*`, `GET /research/latest`, `GET /research/history`, `POST /research/veto`, all `/specs*`, all `/proposals*`, `GET /health/services`. `GET /recommendations` lives in `src/api/recommendations.ts`, not `api/health.ts` (:79). Missing entirely: 35 routers (autopilot-*, attention, attribution, builder-health, capacity, class-stats, config, design-concepts, dispatches, events, explore-page, goals, grounding, holdback, learning, maintenance, merge-lock, metrics-cost, metrics-tokens, now-*, observability, outcomes-page, pattern-memory, reflections, scout, taxonomy, today-page, usage, versions, …). | becomes a generated inventory (routes) |
| REF-07 | 73 | "There is **no** `GET /api/work-queue/<id>` … read it via `GET /api/queue` … `GET /api/queue/snapshot`" | `/api/queue*` gone (#3455). Master has `GET /api/autopilot/work-queue` (measured). | fix in place |
| REF-08 | 82 | "`POST /kill` … writes the `~/hydra/.kill` file that health.ts/service-strip.ts poll" | `src/service-strip.ts` does not exist (no GitHub history for the path). `KILL_FILE` is read in `src/api/health.ts:57` and `src/api/operational.ts:20`. | fix in place |
| REF-09 | 113-141 | "Candidate Feed (ADR-0036)" — `src/anchor-candidates.ts`, `getCandidateFeed`, `GET /api/anchor/candidates`, `src/api/anchor.ts`, `loadBacklog`, `getWorkQueueItems`, `POST /api/queue` | All deleted 2026-07-18 (`191955357`, "retire the Redis backlog subsystem (ADR-0031 contract #3439) (#3455)"); `src/backlog/` absent. ADR-0036 still stands as a decision record; the implementation section here describes deleted code as live. | move to `docs/historical/` |
| REF-10 | 149-151 | Defaults `HYDRA_TARGET_NAME` = `hydra-betting`, `HYDRA_TARGET_GITHUB_REPO` = `gaberoo322/hydra-betting` | `src/target-config.ts:28-29`: `claw-street-bets` / `gaberoo322/claw-street-bets`. | fix in place |
| REF-11 | 152 | `HYDRA_WORKSPACE` "Removed once #259 migrates the last caller" | #259 is **closed**; the shim is still read (`src/target-config.ts` — `grep HYDRA_WORKSPACE`). | fix in place |
| REF-12 | 154 | "`src/target-config.ts` exposes six pure leaf-level helpers" | Seven exported helpers (`getTargetWebUrl` added; `grep -oE '^export (function\|const) '`), plus `__resetForTests`. `HYDRA_TARGET_WEB_URL` (used by `CONTEXT.md:26`) is not in the env table at :147-152. | fix in place |
| REF-13 | 156 | "Migration status: issue #258 adds the helper module only … tracked in issue #259" | #259 closed ("Migrate src/ callers to target-config helper"). | delete |
| REF-14 | 158-171 | Target worktree lifecycle under `~/hydra-betting/web/.worktrees/` | `src/worktree-orphan.ts:5-7,343-346` still says "(hydra-betting)" and scopes to the "hydra-betting worktree family"; Target default is now CSB (README-01). `src/worktree-orphan.ts`, `src/scheduler/chores/worktree-orphan-prune.ts`, `DEFAULT_WORKTREE_MIN_AGE_SECONDS = 6h` (:69) verified. Doc mirrors a code hard-code that post-dates the swap. | fix in place |
| REF-15 | 173-324 | "Codex OpenTelemetry — historical": "`src/codex-otel.ts` … intentionally retained"; `buildTraceUrl`/`isOtelEnabled`/`buildCodexOtelEnv`; "Cycles page (`/cycles`)"; `CODEX_HOME`, `~/.codex/config.toml` | `src/codex-otel.ts` deleted 2026-05-24 (`b4d3fec70`, #581). Survivors: `src/api/observability.ts:39` reads `HYDRA_OTEL_ENABLED`, :43/:59 `HYDRA_TRACE_UI_URL`, routes `GET /observability/config` (:58) and `GET /observability/trace-url` (:70). No `/cycles` dashboard route (`App.jsx`). `scripts/otel/*` (7 files) and `docs/observability/*.json` (2) still exist, orphaned. `CODEX_HOME` has no reader. 152 lines. | move to `docs/historical/` |
| REF-16 | 326-343 | "Merge Gate — historical" | Accurate: `src/gate.ts`, `src/scope-enforcement.ts` absent; `src/mutation.ts` exports `SKIP_PATTERNS, conclusiveMutants, shouldSkipMutation, runMutationTests`. Already marked historical; sits between live sections. | move to `docs/historical/` |
| REF-17 | 345-366 | Modification Tiers table and prose | :352 T2 "`~/.claude/skills/`, `dashboard/`, `src/anchor-selection/`" → classifier: `.claude/skills/` (repo-relative, no such dir), `dashboard/`, file `src/anchor-selection.ts` (deleted). :353 T3 "**Operator merges** (auto-merge unless `scope-justification:`)" → contradicts ADR-0015 / README:68 / `CONTEXT.md:74` (every tier auto-merges). :354 T4 "**Operator only** — `operator-approved` label required; CI blocks otherwise", :360 "`tier-gate` CI job fails any T4 PR without the label", :362 "requires `operator-approved`" → `.github/workflows/ci.yml:131-134` and `scripts/tier-classify.ts:17-21`: tier-gate no longer fails on the label; T4 gate is the base-ref `deep-qa-gate` check (ADR-0020). :366 "exits 2 if a Verifier-Core (T4) path lacks the flag" → `scripts/tier-classify.ts:40-47` the flag is a no-op; :80-94 "classification exits 0". :358 is drift-test-pinned and accurate. | fix in place |
| REF-18 | 368-415 | Outcome Holdback section | Seams verified (`src/holdback.ts`, `src/redis/holdback.ts`, `src/api/holdback.ts`, routes `POST /holdback/{enroll,check,revert-failed}`; events at `src/holdback.ts:6`; `hydra-qa.md:1142` section). Stale: :368 heading "ADR-0004 step 4" (0004 superseded); :398 "Defaults live in `src/redis/holdback.ts`" → `HYDRA_HOLDBACK_WINDOW_CYCLES` in `src/holdback-policy.ts:39` (plus `_T3`=7 :52, `_T4`=10 :53 — undocumented), `HYDRA_HOLDBACK_CYCLE_MS` in `src/outcome-regression.ts`; :400 "#741 layers a tier-aware map" and :415 "Broadening enrollment to T3/T4 … is the sibling issue #741" → #741 **closed** and landed. `POST /api/holdback/pending` (measured) undocumented. | fix in place |
| REF-19 | 417-475 | "Cost reconciliation — historical": "`src/cost/reconciliation.ts` still runs", "`GET /api/cost/reconciliation` … retained", `test/cost-reconciliation.test.mts`, `codex-runner.ts`, `CODEX_HOME` | Module deleted 2026-05-25 (`712f23f01`, #602); route absent (measured); test file absent; `src/redis/cost.ts:130-132` "Reconciliation accessors removed alongside `src/cost/reconciliation.ts`"; `src/codex-runner.ts` absent; `CODEX_HOME` unread. `docs/historical/README.md` already lists "JSONL reconciliation … #602" as retired. 59 lines. | delete |
| REF-20 | 477-497 | GitHub API rate-limit handling | Verified: `src/github/exec.ts` (`classifyFailure`, `isRateLimitStderr`, `gh-rate-limited`), backoff key in `src/redis/oauth-backoff.ts`. | — (verified) |
| REF-21 | 499-509 | Model Tiers table: `claude-fable-5 (1M)` / `claude-sonnet` / `claude-haiku` mapped to skill lists | No code source: `scripts/autopilot/classes.json` has no model column (keys: `name,kind,skill,costClass,learningAgent,cooldownSeconds,scope,provenanceLabel,notes`); `decide.py` carries only `ESCALATION_POLICY` models (`sonnet`/`fable`, :1196-1207) and a `"haiku"` default (:2887). The live per-class model map is prose in `docs/operator-playbooks/hydra-autopilot.md:131-196` (incl. a fable un-entitlement note). Table is unverifiable against code. | becomes a generated inventory (classes) |
| REF-22 | 511-529 | Learning System operational detail | :524 "auto-promotes to a durable lesson file (`~/.claude/skills/<skill>/lessons.md`)" → `src/pattern-memory/agent-memory.ts:27-31` promotion now "stamps `promoted/promotedAt` in the Redis pattern store only"; `src/aggregators/lessons-explorer.ts:20` "never read the on-disk `lessons.md`". :524 "`PROMOTION_THRESHOLD` … exported from `src/pattern-memory/agent-memory.ts`" → `src/pattern-memory/constants.ts:27` (re-exported `index.ts:76`). :527 "`escalation.ts::CUE_ESCALATION_THRESHOLDS`" → `src/pattern-memory/cue-policy.ts:163`. :528 "auto-promotes to `to-planner.md`" → write retired (#2962). Verified: 14-day prune (`agent-memory.ts:535`), 15-slot buffer (`pattern-store.ts:35`), `meta-friction` label (`demotion.ts:70`), `scripts/cleanup/reclassify-deferred-acs.sh` exists. | fix in place |
| REF-23 | 531-542 | Config section | :539 `config/direction/goals.md` deleted 2026-09-16 (`991c21228`). :542 "`config/feedback/to-*.md` files were retired with the codex cut-over" → `config/feedback/to-planner.md` and `to-planner-1125-iotest.md` exist on disk (writer retired, README:80 still tells the operator to edit them). :542 "legacy in-process agent personalities … retired — see `docs/historical/`" → `config/architect.md, builder.md, devops.md, meta.md, reviewer.md, strategist.md, tester.md, researcher.md, research-architect.md, domain-researcher.md, market-researcher.md, technical-researcher.md, research-strategist.md` still sit at `config/` root with no reader in `src/`. Verified: `config/research/` (6 files), `priorities.md` referenced by `hydra-target-research.md:26`. | fix in place |
| REF-24 | 544-621 | Backups: `hydra-pg-backup.{timer,service}` and `hydra-redis-backup.{timer,service}` nightly | Host state (`ls ~/.config/systemd/user/`, `systemctl --user list-unit-files 'hydra*'`): `hydra-pg-backup.timer` exists but is **disabled**; `hydra-redis-backup.*` **not installed**; `~/.local/bin/hydra-redis-backup.sh` absent; `/mnt/hydra-ssd/backups/redis/` **empty**; last Postgres backup `hydra-2026-09-13_0300.sql.gz`. In-repo `scripts/redis-backup.sh` and `hydra-notify-failure@.service` exist. :550 "Postgres (hydra-betting)" pre-dates the swap. | fix in place |
| REF-25 | 623-632 | Deploy recipe (6 steps) | `.github/workflows/ci.yml:381-403` `deploy` job (`if: push && master`, `needs: [test, dashboard-build]`) runs `bash /home/gabe/hydra/scripts/deploy.sh`; `scripts/deploy.sh` does flock (:22), `git pull --ff-only origin master` (:42), `npm ci` (:45), `sync-skills.sh` (:60), dashboard `npm ci && npm run build` (:63), `daemon-reload` (:74), restart (:178), then `scripts/ci/wait-for-health.sh` polling `/api/health` (:180-187). Recipe omits the flock, daemon-reload, retry-probe and the `needs:` gate. Substantively accurate. | becomes a generated inventory (units/scripts) |
| REF-26 | 634-641 | git hook + session-capture hooks | Verified: `scripts/setup-git-hooks.sh`, `scripts/hooks/{extract-dispatch-sentinel,session-start-capture}.sh`, `scripts/autopilot/hooks/on-subagent-tool-call.sh`, `.claude/settings.json:5-11` `SessionStart`, `POST /api/dispatches/subagent`, `HYDRA_DISPATCH_REGISTER_MARKER_DIR`. | — (verified) |
| REF-27 | 5 | "static inventories rot faster than code changes" | The file then carries three static inventories (Redis keys, streams, endpoints) that this audit finds rotted (REF-01..07). | becomes a generated inventory |
| REF-28 | 86 | Target Outcomes heading cites "ADR-0003 + ADR-0004" | ADR-0004 superseded by 0015 (`docs/adr/README.md:18`). Schema table otherwise verified (`holdback` field `src/outcomes.ts:83-146`, `file`-only source, `src/outcomes-yaml.ts`, `test/outcomes-yaml.test.mts`). | fix in place |
| REF-29 | 12, 34, 36, 45 | `cycle:active:{source}` 15-min TTL; 15-slot buffer; 7-day reflections; alerts kept 100 | Verified: `src/api/cycles.ts:99` (900 s), `src/pattern-memory/pattern-store.ts:35`, `src/reflections/per-anchor.ts:42`, `src/api/alerts.ts:25`. | — (verified) |

### 3.3 `CONTEXT.md`

| # | Line | Claim (quoted, short) | What master says (path) | Disposition |
|---|---|---|---|---|
| CTX-01 | 58 | Verifier Core = six files incl. "`.github/workflows/deploy.yml`" | Matches `src/untouchable.ts:2-11` (drift-test-pinned) — but `.github/workflows/deploy.yml` **does not exist** (`ls .github/workflows/` → 16 files, none named deploy) and has no history (`gh api repos/gaberoo322/hydra/commits?path=.github/workflows/deploy.yml` → empty). Deploy is the `deploy` job in `ci.yml:381`. Doc and code agree on a phantom path; the code (T4) must change first, then the four pinned sentences. | fix in place |
| CTX-02 | 66 | Merge Lock "Acquired … via `/api/merge-lock`" | Routes are `POST /api/merge/lock` and `POST /api/merge/unlock` (`src/api/merge-lock.ts`, measured). | fix in place |
| CTX-03 | 70-71 | "Replaced the deleted in-process Outcome Holdback watcher (`src/holdback.ts`, removed in the ADR-0006 cut-over)"; Avoid "holdback watcher (no longer exists as a module)" | `src/holdback.ts` **exists** on master as the request-scoped producer (`enrollHoldback`/`checkHoldback`, `src/holdback.ts:6-30`; `docs/reference.md:375`). Contradicts reference.md and `docs/historical/README.md` (which still lists `src/holdback.ts` as retired). | fix in place |
| CTX-04 | 74 | T2 = "`.claude/skills/`, `dashboard/`, anchor-selection"; T3 "incl. `grounding.ts`" | `src/tier-classifier.ts:67-75` (`.claude/skills/` — no such repo dir; `src/anchor-selection.ts` deleted 2026-06-01). `src/grounding.ts` moved into `src/grounding/` 2026-06-19 (`dc325c7df`, #2132). | fix in place |
| CTX-05 | 94 | Design Concept: "Phase A (issue #438) ships persistence + API only; autopilot wiring is Phase B, CI hook is Phase C" | `.github/workflows/design-concept-comment.yml` and `design-concept-reconcile.yml` exist (CI hook landed; reconcile is a required check per operator memory). | fix in place |
| CTX-06 | 95 | Avoid "spec (overloaded with `src/specs.ts` …)" | `src/specs.ts` deleted 2026-05-19 (`49bc8f36c`, #519). | fix in place |
| CTX-07 | 98-99 | Candidate Feed entry: `GET /api/anchor/candidates`, `src/anchor-candidates.ts`, `src/api/anchor.ts`, `src/backlog/candidate-scoring.ts`, `merged-refs.ts`, `work-queue-hygiene.ts` | All deleted 2026-07-18 (`191955357`, #3455); `src/backlog/` directory absent; no `/api/anchor/*` route. Entry presents a retired subsystem as live (contrast the explicitly RETIRED Knowledge Base entry at :121). | move to `docs/historical/` (or mark RETIRED in place) |
| CTX-08 | 106 | Pattern Memory "Auto-promotes patterns to `config/feedback/to-{agent}.md` at the 3-hit threshold" | `src/pattern-memory/agent-memory.ts:27-31`: the promote→observe→demote lifecycle over `config/feedback/to-*.md` "was retired" (#2962); promotion stamps Redis only. Threshold 3 verified (`constants.ts:27`). | fix in place |
| CTX-09 | 109-111 | **Feedback File** entry — `src/pattern-memory/feedback-file.ts`, `## Auto-Promoted Rules` grammar, `rule-effectiveness.ts` demotion | `src/pattern-memory/feedback-file.ts` deleted 2026-07-07 (`9659da669`, #2975); no `Auto-Promoted Rules` string anywhere in `src/`. | move to `docs/historical/` |
| CTX-10 | 114 | Escalation Outcome surfaced on `/learning/friction-patterns` | `GET /api/learning/friction-patterns` exists (measured). `src/pattern-memory/escalation.ts` exists. | — (verified) |
| CTX-11 | 247 | Relationships: "**Knowledge Base** … is semantic search. All three are composed by `src/learning/composition.ts::getContext()` … a curated Knowledge Base lessons-slice as its own `knowledge-base` block … via a `trackedOvSearch` call … the subagent's own ad-hoc OV HTTP queries … `src/learning.ts` owns none of the three" | Contradicts the same file at :121-123 (Knowledge Base **RETIRED**, "no semantic-similarity tier") and :126 ("the `knowledge-base` block was removed with OpenViking"). `src/learning/composition.ts:17,85,232` confirm removal; `src/learning.ts` deleted 2026-06-20 (`69679090c`). Self-contradiction. | fix in place |
| CTX-12 | 150-151 | **Research Floor** "currently enforced inside the Orchestrator Scheduler (`src/scheduler/research-floor.ts`) … launches a research cycle via `runResearchLoop` … the Scheduler-side floor is being deleted" | `src/scheduler/research-floor.ts` deleted 2026-05-29 (`ff00599bc`, #710); `runResearchLoop` survives only in comments (`src/scheduler/heartbeat.ts:129,427`). The decide.py half is real (`_research_force_allowed` at `decide.py:5989`). | fix in place |
| CTX-13 | 158 | Roadmap Milestone: "`config/direction/roadmap.md` … refreshed by the `hydra-target-research` skill" | `config/direction/roadmap.md` deleted 2026-09-16 (`991c21228`, #4479); `docs/operator-playbooks/hydra-target-research.md` names `priorities.md` (:26), not `roadmap.md`. | fix in place |
| CTX-14 | 162 | Focus Label "Doesn't currently exist as a label family in the issue tracker" | Label `focus-observability` exists on `gaberoo322/hydra` (`gh api repos/gaberoo322/hydra/labels`). | fix in place |
| CTX-15 | 166-167, 251 | **Autopilot Focus** — `hydra:autopilot:focus`, `hydra focus set <kind> <value>` / `hydra focus clear`, `decide.py:_select_for_slot` prefers focus-matching candidates, "Surfaced on the dashboard" | Zero references: `grep -rn 'autopilot:focus' src/ scripts/` → none; `bin/hydra` has no `focus` subcommand (`grep -n focus bin/hydra` → one unrelated comment at :1930); `decide.py` contains the word "focus" once (:1930-adjacent comment), `_select_for_slot` (:4301) has no focus branch. Described as shipped; not implemented. | delete (or mark as proposal) |
| CTX-16 | 170, 182, 186, 194, 198, 202 | "Pre-merge Gate job `github-seam-check`" (:170); `host-probe-seam-check` ratchet (:182); `journal-seam-check` "a step in the consolidated `.github/workflows/seam-checks.yml`" (:186); `anthropic-seam-check` "a step in `.github/workflows/seam-checks.yml`" (:194); "Pre-merge Gate job `redis-seam-check`" (:198); "Pre-merge Gate job `schema-validation-check` … `scripts/ci/schema-validation-baseline.json`" (:202) | `ci.yml` jobs are `test, dashboard-build, tier-gate, mutation-test, scope-check, deploy` — none is a seam check. `seam-checks.yml` deleted 2026-07-22 (`69bafb2af`, #3553). Seam checks run as **advisory** steps in `.github/workflows/advisory-checks.yml` (:239 schema-seam, :243 query-seam, :247 github-seam, :251 host-probe-seam, :255 journal-seam, :259 claude-cli-seam, :263 anthropic-seam, :267 telegram-seam) and `redis-seam-check` in `test-typecheck.yml:51`. `scripts/ci/schema-validation-check.ts` / `schema-validation-baseline.json` do not exist (the live script is `scripts/ci/schema-seam-check.ts`). Contradicts :62 (Pre-merge Gate = `ci.yml` jobs). | fix in place |
| CTX-17 | 199 | Redis Adapters Avoid: "'Redis adapter' (singular — there are 18, not 1)" | `ls src/redis/*.ts \| wc -l` → **45**. | becomes a generated inventory (counts) |
| CTX-18 | 202 | Schemas "First landed Module: `src/schemas/queue.ts` (covers `POST /api/queue`, #562)" | Deleted 2026-07-18 (#3455). 28 modules in `src/schemas/`. `aggregatorRoute` (`src/api/route-helpers.ts:98`) and `src/schemas/common.ts` verified. | fix in place |
| CTX-19 | 210 | Cost "Tier 0 (Untouchable Core)" | Retired naming (:59, :75 in the same file); `src/cost/` is T3 by ADR-0015 (CLAUDE.md table, `src/tier-classifier.ts:13-14`). Retired-module list (`cap.ts`, `attribution.ts`, reconciliation) verified absent. | fix in place |
| CTX-20 | 38 | Health Snapshot probe fan-out includes "patterns/reflections/OV search" | `src/health/diagnostics.ts:183`: "The `svcProbes` service-probe map was removed with OpenViking"; `src/api/health.ts:83` likewise. | fix in place |
| CTX-21 | 146 | Housekeeping "is the six chores moved off the 5-minute tick in #723 … plus the forecast-calibration-brier … producer" | `src/scheduler/chores/` holds **19** files (attribution-self-arm, cycle-merge-reconcile, design-concept-snapshot, glm-eligibility-sweep, holdback-merge-watch, memory-consolidation, pattern-cue-demotion, review-pickup-notify, stale-key-prune, target-outcomes-publish, usage-weekly-snapshot, weekly-digest, wiring-liveness{,-dark-alarm,-outcomes,-output,-timer,-types}, worktree-orphan-prune). `metrics/forecast-calibration-brier.txt` exists. | becomes a generated inventory (chores) |

Verified accurate in `CONTEXT.md` (not findings): Target Manifest env set incl. `HYDRA_TARGET_WEB_URL` (:26; `~/claw-street-bets/.hydra/manifest.json` exists); Health Diagnostic/Assessment in `src/health/diagnostics.ts` (:42, :46); Autopilot Run/Turn module split and routes (:130-135 — `runs.ts`, `sweep-reader.ts`, `run-reads.ts`, `bootstrap.sh`, `term-check.py`, `heartbeat.py`, `POST /api/autopilot/{run-start,run-end,turn}`); 5-minute heartbeat (`heartbeat.ts:125`); `emptyRate` (`src/scheduler/rolling-rates.ts`); `POST /api/maintenance/housekeeping` + `hydra-housekeeping.timer`; GitHub CLI Adapter / Issue-PR Read / Host-Probe / Journal / Anthropic adapters' module paths (`src/github/{exec,gh,git,issues,prs,view-pr}.ts`, `src/host-probe/{exec,probe}.ts`, `src/journal/{exec,read}.ts`, `src/anthropic/request.ts`, `src/cost/oauth-usage.ts`, `PROVENANCE_LABELS` `src/taxonomy/classes.ts:441`, `readMetaFrictionIssues` `src/aggregators/friction-source.ts:54`); Dispatch-Class Taxonomy (`classes.json` 22 rows; `PIPELINE_SLOTS` `decide.py:391`); Subscription Usage Tracker routes and `state.usage_eligibility` (`decide.py:139`); Quota Weight `weightedQuota` (`src/api/class-stats.ts:30`); `percentSinceReset` (`src/digest-fanout.ts:105`); Pace Gate (`hydra-pace-gate.{service,timer}` enabled); Autopilot pause (`src/redis/autopilot-pause.ts`, `GET/POST /api/autopilot/paused`); Reflections 7-day TTL (`src/reflections/per-anchor.ts:42`); "Untouchable Core"/"Tier 0" appear only as Avoid terms (:59, :75, :103).

### 3.4 `CONTEXT-MAP.md`

| # | Line | Claim (quoted, short) | What master says (path) | Disposition |
|---|---|---|---|---|
| MAP-01 | 5, 68, 74 | terms resolve "via `/grill-with-docs`"; "`grill-with-docs` `CONTEXT-FORMAT.md` shape" | Not a deployed skill: `~/.claude/skills/` holds 36 `hydra-*` skills only; `grill-with-docs` exists solely in the plugin cache (`~/.claude/plugins/cache/claude-plugins-official/mattpocock-skills/1.2.3/skills/engineering/grill-with-docs`), not in the session's loaded skill list (which has `mattpocock-skills:grilling` / `domain-modeling`). Hydra's spec stage is `hydra-grill` (ADR-0035, `docs/adr/README.md:49`). | fix in place |
| MAP-02 | 5, 13-32 | "Per-domain `CONTEXT.md` files are created lazily" — 10 rows marked `_lazy_`, 1 row linked | `find . -name CONTEXT.md -not -path '*/node_modules/*'` → exactly `./CONTEXT.md` and `./src/autopilot/CONTEXT.md`. Accurate as written (lazy), but the count is nowhere stated. | becomes a generated inventory (config/docs) |
| MAP-03 | 9 | `CONTEXT.md` "Defines: … **Untouchable Core**, Pre-merge Gate, Merge Lock, …" | `CONTEXT.md:57` defines **Verifier Core**; "Untouchable Core" is an Avoid term (:59). The list also omits ~35 of the ~45 terms the glossary now defines. | fix in place |
| MAP-04 | 29 | "~~`src/anchor-selection/`~~ — RETIRED, directory deleted; **Candidate Feed is the live concept**" | Candidate Feed deleted 2026-07-18 (#3455) — see CTX-07. | fix in place |
| MAP-05 | 41 | Area "`.claude/skills/` + `docs/operator-playbooks/`" | No `.claude/skills/` in the repo (`ls -a .claude/` → `settings.json settings.local.json worktrees`). Skills are generated from `docs/operator-playbooks/` into `~/.claude/skills/` by `scripts/sync-skills.sh`. | fix in place |
| MAP-06 | 55 | roster "is ~6 KB; read it in full" | `wc -c docs/adr/README.md` → 11,772 bytes (~11.5 KB). | fix in place |
| MAP-07 | 21 | `src/scheduler/` glossary terms include "Research Floor" | `src/scheduler/research-floor.ts` deleted (CTX-12). | fix in place |

Verified accurate in `CONTEXT-MAP.md`: every `src/` area in the table exists (`src/host-probe`, `src/redis`, `src/schemas`, `src/cost`, `src/autopilot`, `src/taxonomy`, `src/scheduler`, `src/pattern-memory`, `src/reflections`, `src/metrics`, `src/aggregators`, `src/api`, `src/logger.ts`, `src/target-config.ts`, `src/design-concept.ts`, `src/redis/design-concept.ts`); `scripts/autopilot/{decide.py,classes.json}`, `scripts/test/`, `docs/agents/domain.md` exist; the `ubiquitous-language` label exists; ADR-number cells are test-guarded (`test/adr-roster.test.mts`).

### 3.5 `docs/adr/README.md`

| # | Line | Claim (quoted, short) | What master says (path) | Disposition |
|---|---|---|---|---|
| ADR-01 | 3 | "the corpus is ~280 KB" | `cat docs/adr/[0-9]*.md \| wc -c` → 336,586 bytes (~329 KB, 39 files). | becomes a generated inventory (ADRs) |
| ADR-02 | 27 vs 39, 45 | 0013 row: "hydra-betting is retired as the crucible; Claw Street Bets … succeeds it"; 0025 row: "`hydra-target-incident` never leaves `~/hydra-betting`"; 0031 row: "Target work tracks as GitHub Issues on `gaberoo322/hydra-betting`" | ADR files 0025 and 0031 contain no CSB amendment (`grep -c 'claw\|CSB' …` → 0 each); code default is `claw-street-bets` (`src/target-config.ts:28-29`); `docs/operator-playbooks/hydra-target-incident.md` no longer names `hydra-betting` (0 hits). Roster rows mirror ADR text that the 0013 amendment and the swap have superseded — intra-roster contradiction. | fix in place |
| ADR-03 | 44 vs 49 | 0030 status column "accepted"; 0035 status "accepted (supersedes ADR-0030 Decision 2's spec binding)" | File statuses are the inverse: `0030…md:3` "Accepted (Decision 2's **spec** binding superseded by ADR-0035)"; `0035…md:2` plain `status: accepted`. Similar unmirrored file annotations on 0012/0021/0036 ("live doctrine — corrected from Proposed 2026-08-12"). `test/adr-roster.test.mts` checks only that a Status *exists*, not that it matches the roster. | fix in place |
| ADR-04 | 30 | 0016 row: "recorded post-hoc by autopilot via `cycle-tracking.ts`; `src/task-tracker.ts` is deleted" | `src/cycle-tracking.ts` absent; the module is `src/redis/cycle-tracking.ts` (plus `src/cycle.ts`). `src/task-tracker.ts` absent (accurate). | fix in place |
| ADR-05 | 48 | 0034 row: "five pages organised by operator journey (plus two detail views)" | `dashboard/src/App.jsx:98` mounts a sixth live page `/now` (`NowRoute` → `NowConsole`, :28-34) that ADR-0034:27-37 does not inventory (the ADR says the Now Console/Habitat toggle collapses, :77). Roster mirrors the ADR; code has drifted from both. | becomes a generated inventory (pages) |
| ADR-06 | 38 | 0024 row: "`src/api/ENDPOINT-REGISTRY.md` catalogs the surface" | Registry stale — see §3.6. | becomes a generated inventory (routes) |

Verified accurate in the roster: all 39 rows (0001–0039) resolve to files and every file has a row (test-guarded); status text for 0001 ("superseded-in-part by ADR-0015"), 0004 ("superseded by ADR-0015"), 0005, 0006 ("Accepted, 2026-05-14"), 0009, 0013, 0017, 0020, 0024 (amendment section at `0024…md:181`), 0032 matches the files; "Reserved numbers: none; take 0040+" is consistent with 39 files; `src/untouchable.ts`, `src/tier-policy.ts`, `src/redis/design-concept.ts`, `src/logger.ts`, `scripts/autopilot/decide.py`, `src/design-concept.ts`, `scanArchitecture()` (`src/aggregators/architecture-graph.ts:189`) + `GET /api/architecture`, `pace-gate.service`, `.hydra/manifest.json` (present in both `~/claw-street-bets` and `~/hydra-betting`) all exist.

### 3.6 `src/api/ENDPOINT-REGISTRY.md` (the lead)

| # | Line | Claim (quoted, short) | What master says (path) | Disposition |
|---|---|---|---|---|
| REG-01 | 7 | "**As of:** commit `823eca90c` (origin/master, 2026-09-04)" | The table lists `### src/api/anchor.ts` (:119) and `### src/api/queue.ts` (:375) — both deleted 2026-07-18 (`191955357`, #3455) — and `### src/api/tasks.ts` (:420), deleted 2026-07-11 (`b1456c44b`). The table predates its own stamp by ≥7 weeks. | becomes a generated inventory (routes) |
| REG-02 | 8 | "**Endpoint count:** 147 routes across 47 router modules" | Its own table holds **134** `\| METHOD \| \`path\` \|` rows under **43** `### src/api/*.ts` headings. Master (re-measured with the registry's own recipe, extended to `api.`): **147 routes across 48 files** — 48 `api.use(create…Router())` mounts in `src/api.ts:106-222`, `src/api.ts` itself registers 0 routes. The header number coincides with master by accident; the table does not. | becomes a generated inventory (routes) |
| REG-03 | 101-451 | Endpoint catalog | 10 registry rows absent from master: `GET/POST /api/queue`, `GET /api/queue/snapshot`, `POST /api/queue/reconcile`, `GET /api/anchor/candidates`, `GET /api/agents/status`, `POST /api/agents/:id/pause`, `GET/PUT/DELETE /api/env/*`. 23 master routes absent from the registry: `GET /api/attention/{counts,feed}`, `POST /api/attention/:id/dismiss`, `GET /api/attribution`, `GET /api/attribution/impact`, `GET /api/autopilot/class-stats`, `GET /api/autopilot/hitl-grill`, `GET /api/autopilot/work-queue`, `POST /api/autopilot/board/{close,promote,relabel,reopen}`, `GET /api/metrics/{cost-by-outcome,cost-efficiency,cost-per-merged-pr,instrumentation,session-tokens,unclassified}`, `GET /api/taxonomy/classes`, `GET /api/usage/by-issue`, `POST /api/usage/dispatch-cost`, `GET /api/versions`, `POST /api/holdback/pending`. 8 live router files have no heading: `attention.ts, attribution.ts, class-stats.ts, grounding.ts, metrics-cost.ts, metrics-tokens.ts, taxonomy.ts, versions.ts`. | becomes a generated inventory (routes) |
| REG-04 | 21 | "future dashboard refactors (the 19→4 page simplification)" | ADR-0034 is a 13→7 consolidation (`0034…md:27` "Thirteen surfaces become seven"); README says four questions; App.jsx has six live pages. | fix in place |
| REG-05 | — | (implicit) registry is the ADR-0024 baseline | No file under `test/` references `ENDPOINT-REGISTRY` (0 hits). Unguarded. | becomes a generated inventory (routes) |

---

## 4. Cross-file contradictions (pairs)

| # | Side A | Side B | Code says |
|---|---|---|---|
| X-01 | README:80 "edit `config/feedback/` to correct agent behavior" | `docs/reference.md:542` "`config/feedback/to-*.md` files were retired"; `CONTEXT.md:106,110` say Pattern Memory writes into them | Writer retired (`src/pattern-memory/agent-memory.ts:27-31`); files still on disk; no reader in `src/`. |
| X-02 | README:119 five runtime deps, cites ADR-0014 | CLAUDE.md six deps incl. `pino`, cites ADR-0005 | `package.json` six deps. |
| X-03 | README:83 four questions (Today/Now/Outcomes/Explore) | `docs/adr/README.md:48` five pages + two detail views | `App.jsx:94-119` six live pages + two detail + four redirects. |
| X-04 | README:5,8 · `docs/reference.md:150-151,160,550` · roster :39,:45 — `hydra-betting` | roster :27 (0013) — CSB is the crucible | `src/target-config.ts:28-29` `claw-street-bets`. |
| X-05 | `docs/reference.md:353-354,360,362` — T3 operator merges; T4 needs `operator-approved`, CI blocks | README:68-69 · `CONTEXT.md:74` · roster :29,:34 — every tier auto-merges; T4 gated by deep-QA marker | `ci.yml:131-134`, `scripts/tier-classify.ts:17-21`. |
| X-06 | README:137 "Verifier Core stays operator-only" | README:55,69 (same file) | ADR-0015/0020 (roster :29,:34). |
| X-07 | `CONTEXT.md:70` "`src/holdback.ts` removed" | `docs/reference.md:375` "`src/holdback.ts` — request-scoped producer" | File exists. |
| X-08 | `CONTEXT.md:247` Knowledge Base live semantic search, `trackedOvSearch`, `src/learning.ts` | `CONTEXT.md:121-123,126` Knowledge Base RETIRED, block removed | `src/learning/composition.ts:17,85,232`; `src/learning.ts` deleted. |
| X-09 | `docs/reference.md:30` `hydra:scheduler:daily-spend` live | `docs/reference.md:509` retired | `src/index.ts:39` no live writer. |
| X-10 | `CONTEXT.md:66` `/api/merge-lock` | (code) | `POST /api/merge/lock`, `/api/merge/unlock`. |
| X-11 | `CONTEXT-MAP.md:9` glossary "Defines … Untouchable Core" | `CONTEXT.md:57-59` defines Verifier Core; Untouchable Core is Avoid | — |
| X-12 | README:46,79 · `docs/reference.md:113-141` · `CONTEXT.md:98` · `CONTEXT-MAP.md:29` — Candidate Feed / `/api/queue` live | (mutually consistent) | All deleted 2026-07-18 (#3455). |
| X-13 | `CONTEXT.md:170,182,186,194,198,202` "Pre-merge Gate job *-seam-check" / `seam-checks.yml` | `CONTEXT.md:62` Pre-merge Gate = `ci.yml` jobs | Seam checks are advisory (`advisory-checks.yml:239-267`, `test-typecheck.yml:51`); `seam-checks.yml` deleted 2026-07-22. |
| X-14 | `docs/reference.md:398` holdback defaults "live in `src/redis/holdback.ts`" | (code) | Split across `src/holdback-policy.ts:39-53`, `src/redis/holdback.ts:50,58`, `src/outcome-regression.ts`. |
| X-15 | `src/api/ENDPOINT-REGISTRY.md:8` 147 routes / 47 modules | its own table 134 rows / 43 headings | Master 147 / 48. |
| X-16 | roster :44 (0030 "accepted") vs :49 (0035 carries the supersession) | ADR files carry the annotation the other way round | — |
| X-17 | `docs/reference.md:352` T2 "`src/anchor-selection/`" · README:67 same | `CONTEXT-MAP.md:29` "`src/anchor-selection/` RETIRED, directory deleted" | Classifier lists the *file* `src/anchor-selection.ts` (deleted); there was never a directory in the classifier. |

---

## 5. Measured counts

| Item | Stated where | Stated | Measured | How measured |
|---|---|---|---|---|
| ADR files | ticket charting (2026-09-17) | 40 | **39** (0001–0039) | `ls docs/adr/[0-9]*.md \| wc -l` |
| ADR corpus size | `docs/adr/README.md:3` | ~280 KB | **336,586 B (~329 KB)** | `cat docs/adr/[0-9]*.md \| wc -c` |
| Roster size | `CONTEXT-MAP.md:55` | ~6 KB | **11,772 B** | `wc -c docs/adr/README.md` |
| Playbooks | ticket charting | 51 | **57** `.md` total; 38 top-level; 36 `hydra-*.md` | `find docs/operator-playbooks -name '*.md' \| wc -l`; `ls docs/operator-playbooks/*.md \| wc -l`; `find … -maxdepth 1 -name 'hydra-*.md' \| wc -l` |
| Deployed `hydra-*` skills | ticket charting | 36 | **36** | `ls -d ~/.claude/skills/hydra-* \| wc -l` |
| Co-located `CONTEXT.md` | `CONTEXT-MAP.md:13-32` | 1 linked + 10 `_lazy_` | **2** (`./CONTEXT.md`, `src/autopilot/CONTEXT.md`) | `find . -name CONTEXT.md -not -path '*/node_modules/*'` |
| API routes / routers | `ENDPOINT-REGISTRY.md:8` | 147 / 47 (table: 134 / 43) | **147 / 48** | registry recipe extended to `(router\|app\|api)\.` over `src/api.ts` + `src/api/*.ts` minus `route-helpers.ts`; `grep -c 'api.use(create' src/api.ts` = 48 |
| Routers documented in `reference.md` | `docs/reference.md:71-84` | 13 | 48 on master | as above |
| Dashboard pages | README:83 / ADR-0034 / roster :48 | 4 questions / 5 + 2 | **6 live + 2 detail + 4 redirects** | `grep -n '<Route' dashboard/src/App.jsx` |
| Redis key rows | `docs/reference.md:9-52` | 42 | **226** distinct `hydra:` literals in `src/`; **74** builders in `src/redis/keys.ts`; **26** builders with zero callers | `grep -rhoE 'hydra:[…]' src/ \| sort -u \| wc -l`; `grep -cE '^\s+[a-zA-Z0-9]+:\s*\(' src/redis/keys.ts`; per-builder `grep -rE 'redisKeys\.<b>\b'` |
| Event streams | `docs/reference.md:58-63` | 4 | **2 live** (`notifications`, `dlq`) + 1 retained (`cycle`) | `src/event-bus-stream-keys.ts:45-66` |
| Consumer groups | `docs/reference.md:65` | 4 | **2** (`telegram`, `dlq-processor`) | `src/event-bus-stream-keys.ts:85-88` |
| Verifier Core paths | README:69 | 4 | **6** | `src/untouchable.ts:2-11` |
| Redis adapters | `CONTEXT.md:199` | 18 | **45** | `ls src/redis/*.ts \| wc -l` |
| Schema modules | — | — | 28 | `ls src/schemas/*.ts \| wc -l` |
| Runtime deps | README:119 | 5 | **6** | `node -e 'Object.keys(require("./package.json").dependencies)'` |
| Node version | README:89 | ≥ 22 | `>=22.18.0 <23.0.0`; `.nvmrc` 22.23.1 | `package.json` `engines`; `cat .nvmrc` |
| Tests | README:113 | ~3,500 | 8,236 `test(`/`it(` sites; 1,962 baseline top-level entries in 466 files | see README-13 |
| Housekeeping chores | `CONTEXT.md:146` | 6 + 1 | **19** files | `ls src/scheduler/chores/` |
| Target-config helpers | `docs/reference.md:154` | 6 | **7** (+ `__resetForTests`) | `grep -oE '^export (function\|const) ' src/target-config.ts` |
| Env vars read by code | `docs/reference.md` tables (~30 named) | — | **196** distinct names in `src/` + `scripts/` | `grep -rhoE 'process\.env\.[A-Z0-9_]+\|\$\{?(HYDRA\|OTEL\|CODEX)…' src/ scripts/ \| sort -u \| wc -l` |
| `hydra*` systemd units on host | `docs/reference.md:548-555` names 6 | — | **31 services + 22 timers** (incl. 27 `hydra-betting-*`); no `hydra-redis-backup.*` | `systemctl --user list-unit-files 'hydra*'` |
| "historical" sections in `reference.md` | ticket lead | 3 marked | 3 marked (:173, :326, :417) + 1 unmarked describing deleted code (:113 Candidate Feed) + 1 partially stale policy block (:345) | this audit |
| Files named in the five docs that do not exist | — | — | **26** (see §3; deletion dates via `gh api repos/…/commits?path=`) | `test -e` over every named path |

---

## 6. Hand-offs to #4542 (findings whose disposition is "becomes a generated inventory")

The 18 "becomes a generated inventory" findings, grouped by inventory type. Items in
*(related)* carry a different disposition but are the inputs the same extractor would consume.

- **Routes (7):** REF-06, REF-27, REG-01, REG-02, REG-03, REG-05, ADR-06. *(related: REF-07 fix.)* Source of truth: `src/api.ts` mounts + `(router|app|api).<verb>("…")` literals in `src/api/*.ts`. The registry's own recipe (`ENDPOINT-REGISTRY.md:38-69`) is a working extractor once `api.` is added to the regex; the *table* is what rots.
- **Pages (2):** README-11, ADR-05. Source: `<Route path=…>` in `dashboard/src/App.jsx` (distinguish live page / detail / `<Navigate>` redirect).
- **Classes and model routing (1):** REF-21. Source: `scripts/autopilot/classes.json` (22 rows; no model column) + the model map in `docs/operator-playbooks/hydra-autopilot.md` — the latter is prose and needs a committed artifact before it can be inventoried.
- **Skills (0):** no generated-inventory finding; *(related: MAP-05 fix — where skills live: `docs/operator-playbooks/hydra-*.md` (36) ↔ `~/.claude/skills/hydra-*` (36)).*
- **Config / docs layout (1):** MAP-02 (co-located `CONTEXT.md` list). *(related: REF-23 fix — what is actually under `config/`: 13 unread personality files at the root; `config/direction/` now 7 files without `goals.md`/`roadmap.md`.)*
- **Redis keys (1):** REF-01. *(related: REF-02/REF-03 delete, REF-04 fix — the retirement list.)* Source: `src/redis/keys.ts` builders + call-site count; live families absent from the doc: `autopilot:*`, `attention:*`, `scout:*`, `retro:*`, `dispatches:*`, `holdback:*`, `cost:*`, `design-concept:*`, `friction:*`, `learning:*`, `review:pickup-armed`, `scheduler:{cycles-*,state-version,deliberate-stop,research-*}`, `metrics:{tokens:*,claims-reaped*,usage-snapshot:*}`, `cleanup:last-daily`, `agent-stream`, `backlog:{reconciler:health,title-index}`.
- **Streams (1):** REF-05. Source: `src/event-bus-stream-keys.ts` (`STREAMS`, `RETAINED_STREAMS`, `CONSUMER_GROUPS`).
- **CI gates, required vs advisory (0):** no generated-inventory finding; *(related: CTX-16 fix — which check runs where; the Pre-merge Gate job list at `CONTEXT.md:62`).* Source if generated: `.github/workflows/ci.yml` job ids vs `advisory-checks.yml` / `test-typecheck.yml` step names.
- **Units / scripts (1):** REF-25 (deploy steps from `scripts/deploy.sh`). *(related: REF-24 fix — host units actually installed/enabled; README-07 / CTX-01 fix — Verifier Core list from `src/untouchable.ts`.)*
- **ADRs (1):** ADR-01 (corpus size). *(related: ADR-03 fix — status-column parity with each file's `Status` line; extend `test/adr-roster.test.mts` or generate the column.)*
- **Counts (3):** README-13 (test count from the suite-count capture), CTX-17 (adapter count), CTX-21 (chore count).

---

## 7. Verified-accurate (coverage note)

Checked and found correct on `06d30e1cd`, so the reader knows the audit's reach beyond its hits:

- **Paths (137 named paths tested with `test -e`)** — 111 exist, incl. every `src/{github,host-probe,journal,anthropic,cost,pattern-memory,reflections,health,autopilot,scheduler,taxonomy,redis,schemas}` module cited, all `scripts/ci/*-seam-check.ts` cited except the non-existent `schema-validation-check.ts`, all `scripts/autopilot/*` and `scripts/hooks/*` cited, `scripts/{deploy,sync-skills,setup-git-hooks,redis-backup,hydra-watchdog}.sh`, `scripts/tier-classify.ts`, `docs/{quality-gates,evals,codex-removal-measurement}.md`, `docs/agents/*`, `docs/observability/*.json`, `scripts/otel/*`, `config/direction/{vision.md,outcomes.yaml,priorities.md}`, `config/orchestrator/vision.md`, `config/research/`, `config/glm/drainer-settings.json`, `.env.example`, `docker-compose.yml`, `LICENSE`, `dashboard/dist/`, `metrics/forecast-calibration-brier.txt`, `test/fixtures/adr-area-baseline.json`, `test/outcomes-yaml.test.mts`.
- **Routes present as documented:** `/api/health`, `/api/health/deep`, `/api/recommendations`, `/api/scheduler/{start,stop,status}`, `/api/cycle/{status,history,register,complete}`, `/api/research/start`, `/api/metrics`, `/api/summary`, `/api/metrics/record`, `/api/tier`, `/api/digest/{send,heartbeat}`, `/api/kill`, `/api/outcomes`, `/api/holdback/{enroll,check,revert-failed}`, `/api/observability/config`, `/api/learning/{context-trace,friction-patterns}`, `/api/reflections`, `/api/usage`, `/api/usage/eligibility`, `/api/autopilot/{class-stats,paused,run-start,run-end,turn}`, `/api/maintenance/housekeeping`, `/api/dispatches/subagent`, `/api/architecture`, `/api/design-concepts`, `/api/agents/stream`.
- **Env vars with a live reader:** every `HYDRA_HOLDBACK_*`, `HYDRA_USAGE_*`, `HYDRA_TARGET_*`, `HYDRA_PROJECT_WORKSPACE`, `HYDRA_WORKSPACE` (shim), `HYDRA_GH_BIN`, `HYDRA_GIT_BIN`, `HYDRA_GITHUB_REPO`, `HYDRA_AUTOPILOT_JOURNAL_CMD`, `ANTHROPIC_API_KEY`, `HYDRA_ROOT`, `HYDRA_DISPATCH_REGISTER_MARKER_DIR`, `HYDRA_OTEL_ENABLED`, `HYDRA_TRACE_UI_URL`. Without a reader: `CODEX_HOME`; `OTEL_*` appear only in `scripts/otel/` examples.
- **Systemd units named and present:** `hydra-orchestrator.service`, `hydra-housekeeping.{service,timer}`, `hydra-pace-gate.{service,timer}`, `hydra-notify-failure@.service`, `hydra-pg-backup.{service,timer}` (timer disabled), `hydra-watchdog.{service,timer}`, `hydra-autopilot.service`, `hydra-glm-drainer.{service,timer}`, `hydra-branch-prune.{service,timer}`, `${HYDRA_TARGET_NAME}-web.service` pattern (`hydra-betting-web.service` present).
- **Constants:** 5-min heartbeat; 900 s cycle-source TTL; 7-day reflection TTL; 15-slot pattern buffer; 14-day stale prune; `PROMOTION_THRESHOLD = 3`; `DEFAULT_WORKTREE_MIN_AGE_SECONDS = 6h`; `ALERTS_MAX = 100`; `HOLDBACK_WINDOW_CYCLES` 5 / `MAX_REVERTS_PER_DAY` 3; 6 Verifier-Core paths (as listed, phantom `deploy.yml` included).
- **Labels:** `operator-approved`, `meta-friction`, `needs-design-concept`, `hitl-grill`, `ubiquitous-language`, `focus-observability` exist on `gaberoo322/hydra`.
- **Roster structure:** 39 rows ↔ 39 files, unique numbers, every file declares a Status (test-guarded); the amendment notes on 0005/0009/0013/0017/0020/0024/0032 match the files.
- **Historical framing that is correct:** `docs/reference.md:326-343` (Merge Gate), `CONTEXT.md:121-123` and `:189-191` (Knowledge Base / OV adapter RETIRED), `CONTEXT-MAP.md:28` (knowledge-base row), `docs/reference.md:517-522` (OV learning tier note).

---

## 8. Method & limits

**Primary sources only.** Every claim was checked against the working tree at `06d30e1cd` and the live host, never against another doc. Commands used (all read-only):

- Existence: `test -e` / `ls` over every path named in the five files (137 paths).
- Routes: a `.cjs` port of the registry's own recipe (`(router|app|api)\.(get|post|put|delete|patch)\(\s*["'`]…`) over `src/api.ts` + `src/api/*.ts` minus `route-helpers.ts`; `grep -c 'api.use(create' src/api.ts`; `comm` against the registry's `| METHOD | \`path\` |` rows.
- Pages: `grep -n '<Route' dashboard/src/App.jsx`.
- Redis keys: `grep -rhoE 'hydra:…' src/` (226 literals); per-builder call-site counts across `src/` excluding `src/redis/keys.ts`; `src/event-bus-stream-keys.ts` read directly.
- Env vars: `grep -rhoE 'process\.env\.[A-Z0-9_]+' src/ scripts/` plus per-name reader lookup.
- systemd: `ls ~/.config/systemd/user/ | grep -i hydra`; `systemctl --user list-unit-files 'hydra*'`; `ls /mnt/hydra-ssd/backups/*`.
- Counts: `ls docs/adr/[0-9]*.md | wc -l`; `find docs/operator-playbooks -name '*.md' | wc -l`; `ls -d ~/.claude/skills/hydra-* | wc -l`; `find . -name CONTEXT.md -not -path '*/node_modules/*'`; `wc -c`; `ls src/redis/*.ts | wc -l`.
- ADR status: `grep -m1 -in 'status' docs/adr/NNNN-*.md` per file vs the roster's Status column; `test/adr-roster.test.mts` read to learn what is guarded.
- Deletion dates: the local clone is **shallow** (`git rev-parse --is-shallow-repository` → `true`, 377 commits), so `git log -- <path>` returns nothing for the deleted files; dates were taken from `gh api "repos/gaberoo322/hydra/commits?path=<path>&per_page=1"` (last commit touching the path = its deletion). Three paths returned no history at all (`.github/workflows/deploy.yml`, `src/service-strip.ts`, `scripts/ci/schema-validation-check.ts`) — they have never existed on `master`.
- GitHub state: `gh api repos/gaberoo322/hydra/labels`, `…/issues/{259,741}`.
- No subagents were used; every finding was traced by the author.

**Not checked, and why:**

- **`npm test` was not run** (audit constraint), so README's "~3,500 tests" could only be bounded by static counts (README-13).
- **The service was not started**; route existence is from source, not from a live `curl`. The two public URLs were probed with `curl -I` only (both 302).
- **`CLAUDE.md`, `docs/quality-gates.md`, the playbooks and the ADR bodies** were out of scope except where a roster row or a drift test points at them; contradictions with CLAUDE.md are noted only where README quotes the same fact (X-02).
- **`docs/reference.md` prose that describes behaviour rather than a named artifact** (e.g. holdback behaviour guarantees :405-411, rate-limit backoff ladder :493) was checked for the named files/keys/env vars only, not re-derived from the implementation.
- **Value-level TTLs not verified:** merge-lock 60 s (`acquireMergeLock` takes `ttlSeconds` from the caller), autopilot-run 7-day TTL (`RUN_TTL_SECONDS` exists, value not read), reflection 30-day extension.
- **Model routing:** whether the playbook's model map matches `docs/reference.md:503-507` was not adjudicated — there is no committed machine-readable source to compare against (REF-21).
- **Target-side facts** (`~/claw-street-bets`, `~/hydra-betting`) were checked for the manifest file only.
