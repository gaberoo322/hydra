{
  "errorCount": 8,
  "note": "Auto-generated by scripts/ci/test-typecheck-check.ts --write-baseline. Issue #750 closure ratchet: shrink only. This is the number of `tsc` type errors in test/**, which tsx (transpile-only) and the src-only `npm run typecheck` both miss. CI fails only when the count RISES; drive it down over time and re-run --write-baseline."
}
